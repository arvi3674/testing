pipeline{

    agent { label 'linux-slave1' }

    environment {
        dockerImage = ''
    }

    stages {
        
        stage("Display parent git url") {
            steps{
                sh "echo ${GIT_URL}"
            }
        }
        
    }
}